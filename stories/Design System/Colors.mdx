import { Unstyled } from '@storybook/blocks'

export const Colors = {
  Greyscale: [
    'white',
    'whale',
    'athens',
    'porcelain',
    'mystic',
    'casper',
    'waterloo',
    'fiord',
    'black',
  ],
  Green: ['green', 'green-hover', 'green-light-1', 'green-light-2', 'green-light-3'],
  Red: ['red', 'red-hover', 'red-light-1', 'red-light-2'],
  Yellow: ['yellow', 'yellow-hover'],
  Orange: [
    'orange',
    'orange-hover',
    'orange-light-1',
    'orange-light-2',
    'orange-light-3',
    'orange-pale',
  ],
  Blue: ['blue', 'blue-hover', 'blue-light-1', 'blue-light-2'],
  Purple: [
    'purple',
    'purple-hover',
    'purple-light-1',
    'purple-light-2',
    'purple-light-3',
    'purple-light-4',
  ],
}

<Unstyled>
{Object.entries(Colors).map(([group, colors]) => (

<div key={group} className='column mrg-xl mrg--b'>
  <div className='h3 mrg-s mrg--b'>{group}</div>
  <div className='row'>
    {colors.map((color) => (
      <div key={color} className='column color gap-xs monospace'>
        <div className='border' style={{ background: `var(--${color})` }} />
        <div >
          var(--{color})
        </div>
        <div className='c-waterloo'>
          {getComputedStyle(document.documentElement).getPropertyValue(`--${color}`).toUpperCase()}
        </div>
      </div>
    ))}

  </div>
</div>

))}

</Unstyled>

<style>
  {`
  .row {
    gap: 16px;
    flex-wrap: wrap;
  }

  .border {
    width: 160px;
    height: 100px;
  }

  .value {
    font-family: Monospace;
  }
  `}
</style>
