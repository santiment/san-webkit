import { Meta, Unstyled } from '@storybook/blocks'
import tailwindConfig from '../../../tailwind.config.js'

<Unstyled>

export const texts = {
  'text-5xl': 'H1',
  'text-4xl': 'H2',
  'text-3xl': 'H3',
  'text-2xl': 'H4',
  'text-xl': 'H5',
  'text-lg': 'Body 1',
  'text-base': 'Body 2',
  'text-sm': 'Body 3',
  'text-xs': 'Caption',
  'text-2xs': 'Tiny',
}

<div class="flex flex-col gap-8">
  {Object.entries(tailwindConfig.theme.fontSize).map(([text, [fontSize, lineHeight]]) => (
    <div>
      <span class="font-mono mb-3 text-sm font-medium text-waterloo">
        {texts['text-' + text]} - {fontSize} / {lineHeight} - <code>.text-{text}</code>
      </span>
      <p class={'ml-5 ' + 'text-' + text}>The quick brown fox jumps over the lazy dog.</p>
    </div>
  ))}
</div>

</Unstyled>
