export declare function copy(
  text: string,
  callback: () => void,
  timeout?: number,
  node?: HTMLInputElement | HTMLTextAreaElement,
): () => void
export declare const map: (
  target: number,
  valueMin: number,
  valueMax: number,
  targetMin: number,
  targetMax: number,
) => number
